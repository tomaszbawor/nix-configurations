{
  username,
  inputs,
  config,
  lib,
  system,
  pkgs,
  ...
}:
{
  # macOS (darwin) specific home-manager modules
  # Import or configure any home-manager modules you want applied only on macOS here.
  # Example:
  # home.packages = with pkgs; [ starship ];
}
